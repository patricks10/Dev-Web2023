const boasVindas = () => {
    alert('Bem vindo a página da atividade 08');
    console.log('Bem vindo a página da atividade 08');
};

const conta = (a,b,c) => {
    const delta = b * b - 4 * a * c;
const x1 = (-b + Math.sqrt(delta)) / (2 * a);
const x2 = (-b - Math.sqrt(delta)) / (2 * a);
console.log('valor do x1', x1,'Valor do x2',x2)
};